{
  "name": "@gatekeeper/integration-tests",
  "version": "0.1.0",
  "description": "Integration tests for Gatekeeper Docker stack",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@vitest/ui": "^1.6.1",
    "vitest": "^1.6.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}